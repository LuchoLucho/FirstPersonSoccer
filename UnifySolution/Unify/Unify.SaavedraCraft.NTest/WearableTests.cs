using System;
using System.Collections.Generic;
using NUnit.Framework;
using QuarentineSurvival.Model.Actions;
using SaavedraCraft.Model.Interface;
using SaavedraCraft.Model.Interfaces;

namespace Unify.SaavedraCraft.NTest
{
    [TestFixture()]
    public class WearableTests
    {
        /*
        //Holder should update its position/velocity for its beloging too!
        public interface ITangibleActionableHolder<T> : IActionExecutor<T>
        {
            List<IObject<T>> ShowAvailableSpacesToWear();
            List<IWearableActionable<T>> ShowCarriedActionables();
            void WearActionable(IWearableActionable<T> newItem); // We pickup a actionable that was on a actionable Medium or IEnvironment
            void TakeOffActionable(IWearableActionable<T> toDropItem); // We put it back to a IEnvironment?
        }

        

        //Example Weapon, backpack, shield
        public interface IWearableActionable<T> : IPickableDroppable<T>
        {

        }  ------- I commented this since it was in the PickAndDrop that they are still on testing! */
    }
}
